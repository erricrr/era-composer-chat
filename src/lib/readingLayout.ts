/** Comfortable line length for biography and chat prose (~65ch). */
export const readableProseClass = "max-w-prose mx-auto w-full";

/** Centers primary reading columns on large viewports (matches full-view chat). */
export const readablePanelClass = "lg:max-w-5xl lg:mx-auto lg:w-full";
